import 'package:flutter/material.dart';

class CapacitacionPage extends StatelessWidget {        // cambia el nombre según el archivo
  const CapacitacionPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Próximamente'));
}
import 'package:flutter/material.dart';

class TrabajadoresPage extends StatelessWidget {        // cambia el nombre según el archivo
  const TrabajadoresPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Próximamente'));
}